In Go func that begin with capital letters are exported outside the package while that that begins with lowercases aren't they are only available within the scope(inside the file) of where they are defined.

